import { RegisterForm } from "@/features/auth/register-form";

export const metadata = { title: "Inscription - Poker Tracker Pro" };

export default function RegisterPage() {
  return <RegisterForm />;
}
